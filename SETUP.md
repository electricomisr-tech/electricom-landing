# מדריך הקמה - אלקטריקום מערכות

מדריך זה מסביר איך להפעיל את הפאנל הניהולי ולחבר אותו לאתר. צריך לעשות זאת פעם אחת בלבד.

מה נעשה:
1. נקים פרויקט חינמי ב-Supabase (מסד נתונים + אחסון תמונות)
2. נריץ קובץ SQL שיגדיר את כל מה שצריך
3. ניצור משתמש אדמין שיוכל להתחבר לפאנל
4. נחבר את הקבצים שלך לפרויקט ב-Supabase
5. נעלה הכל ל-GitHub ונפרסם

---

## חלק 1 - פתיחת פרויקט ב-Supabase

1. כנס ל-[https://supabase.com](https://supabase.com) ולחץ על **Start your project**
2. הירשם עם חשבון Google או GitHub (חינם)
3. אחרי הכניסה, לחץ על **New Project**
4. מלא:
   - **Name**: `electricom-landing` (או כל שם שתבחר)
   - **Database password**: תיצור סיסמה חזקה ושמור אותה במקום בטוח (לא נצטרך אותה כרגע אבל חשוב לשמור)
   - **Region**: `Central EU (Frankfurt)` (הכי קרוב לישראל)
5. לחץ **Create new project** והמתן 1-2 דקות עד שהפרויקט מוכן

## חלק 2 - הרצת קובץ ה-SQL

1. בתפריט הצדדי של הפרויקט ב-Supabase, לחץ על **SQL Editor** (אייקון של מסד נתונים)
2. לחץ **+ New query**
3. פתח את הקובץ `setup-database.sql` שבתיקייה (לחיצה כפולה)
4. העתק את כל התוכן (`Ctrl+A` ואז `Ctrl+C`) והדבק בעורך ב-Supabase
5. לחץ **Run** (או `Ctrl+Enter`)
6. אם הכל תקין, תראה הודעה ירוקה "Success. No rows returned"

זה יצר עבורך:
- טבלה `site_content` שתכיל את כל התוכן באתר
- bucket אחסון בשם `media` להעלאת תמונות
- הרשאות גישה (כולם יכולים לקרוא, רק אתה יכול לערוך)

## חלק 3 - יצירת משתמש אדמין

זה המשתמש שאיתו תתחבר לפאנל הניהולי.

1. בתפריט הצדדי לחץ על **Authentication**
2. לחץ על הלשונית **Users** ואז על **Add user** > **Create new user**
3. הזן:
   - **Email**: האימייל שלך
   - **Password**: סיסמה חזקה שתזכור (זאת הסיסמה לפאנל)
   - **Auto Confirm User**: ✓ סמן (כדי שלא יצטרך לאשר אימייל)
4. לחץ **Create user**

## חלק 4 - חיבור הקבצים שלך

1. בתפריט הצדדי לחץ על **Project Settings** (גלגל שיניים בתחתית) ואז על **API**
2. תראה שני ערכים שצריך להעתיק:
   - **Project URL** (למשל: `https://abcdefghij.supabase.co`)
   - **anon public** key (מחרוזת ארוכה שמתחילה ב-`eyJ...`)

3. פתח את הקובץ `config.js` בתיקייה שלך וערוך אותו:

```js
window.ELECTRICOM_CONFIG = {
  SUPABASE_URL: 'https://abcdefghij.supabase.co',
  SUPABASE_ANON_KEY: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
};
```

4. שמור את הקובץ.

## חלק 5 - בדיקה מקומית

1. לחץ פעמיים על `admin.html`
2. אמור להיפתח דף עם טופס התחברות
3. הזן את האימייל והסיסמה שיצרת בחלק 3
4. אם הכל עובד, תיכנס לפאנל הניהול. תוכל לערוך כל טקסט וללחוץ **שמור שינויים**
5. פתח את `index.html` בטאב חדש - אמור להציג את הטקסטים שערכת

⚠️ **חשוב**: אם הדפדפן חוסם את הקבצים בגלל שהם נפתחים ישירות (`file://`), פתח אותם דרך שרת מקומי פשוט. הדרך הקלה ביותר ב-Windows:
- פתח PowerShell בתיקייה (לחץ Shift+ימני וגי "Open PowerShell here")
- הרץ: `python -m http.server 8000`
- פתח בדפדפן: `http://localhost:8000/admin.html`

## חלק 6 - העלאה ל-GitHub והפצה חינמית

1. צור חשבון ב-[github.com](https://github.com) אם אין לך
2. ב-GitHub לחץ על **+** ימני למעלה > **New repository**
3. שם: `electricom-landing` (או כל שם), בחר **Public**, ולחץ **Create repository**
4. בדף הבא, GitHub יראה הוראות. תחת **"…or upload an existing repository from the command line"**, לחץ **uploading an existing file** ופשוט גרור את כל הקבצים מהתיקייה לדפדפן ולחץ **Commit changes**
5. אחרי שהקבצים עלו, בתפריט הריפו לחץ **Settings** > **Pages**
6. תחת **Source** בחר: Branch `main`, Folder `/ (root)`, ולחץ **Save**
7. תוך 1-2 דקות יופיע בראש הדף קישור כמו `https://yourusername.github.io/electricom-landing/`

זהו! האתר חי. כדי לערוך תוכן בעתיד:
- כנס ל-`https://yourusername.github.io/electricom-landing/admin.html`
- התחבר עם האימייל והסיסמה שיצרת
- ערוך מה שתרצה, לחץ "שמור שינויים"
- העדכון יופיע באתר תוך שניות (בלי לפרסם מחדש ב-GitHub)

---

## מבנה הקבצים

| קובץ | מה הוא עושה |
|------|------------|
| `index.html` | הדף הראשי שמבקרים רואים |
| `admin.html` | פאנל הניהול הפרטי שלך |
| `config.js` | הגדרות החיבור ל-Supabase (URL + anon key) |
| `cms.js` | סקריפט שטוען את התוכן מ-Supabase לדף |
| `setup-database.sql` | קובץ SQL שמגדיר את ה-DB ב-Supabase (משתמשים בו פעם אחת) |
| `SETUP.md` | המדריך הזה |

## דברים שאפשר לערוך מהפאנל

- ✏️ פרטי קשר (טלפון, אימייל, וואטסאפ) - משתנים בכל הדף בו-זמנית
- ✏️ Hero - כותרת, תיאור, כפתורים, מספרי סטטיסטיקה
- ✏️ אודות - פסקאות, בולטים, תג צד
- ✏️ שירותים - 3 קלפי השירותים (טקסט ובולטים)
- ✏️ למה אנחנו - 4 המאפיינים
- ✏️ פרויקטים - 6 קלפי פרויקטים, כולל **העלאת תמונה** לכל פרויקט
- ✏️ המלצות - הוספה/מחיקה/עריכת המלצות, דירוג כוכבים
- ✏️ סקציית "צור קשר" וטקסטי וואטסאפ
- ✏️ Footer

## בעיות נפוצות

**"Supabase לא מוגדר" כשנכנסים ל-admin.html**
→ שכחת לערוך את `config.js`. ראה חלק 4.

**"התחברות נכשלה"**
→ או שטעית באימייל/סיסמה, או שלא יצרת את המשתמש בחלק 3. נסה לחזור על השלב.

**שמירה נכשלה**
→ ייתכן שה-SQL לא רץ. חזור על חלק 2.

**העדכונים לא מופיעים באתר אחרי שמירה**
→ נסה לרענן את הדף (`Ctrl+F5`). אם עדיין לא, פתח את הקונסולה (F12) וחפש שגיאות אדומות.
